/*when ever the JS program is executed an "execution context" is created
 along with that a "this" keyword is created which points to the window object 
 even for the functional execution context and even for the global execution context */